# task notes
In the result of this test we must execute shell command:
- [`echo -n "hello " && echo "world"`](./main.files/cmd-retcode=0.log)
- [`undefined`](./main.files/cmd0-retcode=127.log)
- ![`echo "hello world"`](<no image in clipboard>)
